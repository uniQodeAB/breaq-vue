language: node_js

node_js:
  - lts/*

install:
  - yarn install
  - cd functions
  - yarn install
  - cd ..
  - yarn build

# before_deploy: "echo 'ready to deploy'"
# deploy:
#   provider: firebase
#   token:
#     secure: "YOUR ENCRYPTED token"
